interface:
  display_name: "Share on Pages"
  short_description: "Publish a local web artifact to your Cloudflare account"
  default_prompt: "Use $share-on-pages to publish this HTML artifact with my configured visibility and return a verified share link."
